"use strict";
// Enables strict mode — helps prevent common JavaScript mistakes 
// and makes the code safer and easier to debug.

let messages = [
    "A change of environment can be a good thing!",
    "You will make it!",
    "Just run the code!"
];
// Creates an array called 'messages' that contains three motivational strings.

messages.forEach(message => console.log(message));
// Loops through each item (message) in the 'messages' array
// and prints it to the console using console.log().
